// Show success popup after updating a user
function showSuccessPopup(message) {
    alert(message);
}

// Redirect to admin dashboard
function redirectToAdminDashboard() {
    window.location.href = "AdminDashboard.aspx";
}